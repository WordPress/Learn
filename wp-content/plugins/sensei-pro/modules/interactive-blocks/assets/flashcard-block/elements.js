/**
 * Internal dependencies
 */
import { createBemComponent } from '../shared/bem';

export const CardWrapper = createBemComponent( {
	className: 'sensei-lms-flashcards__card-wrapper',
} );

export const Card = createBemComponent( {
	className: 'sensei-lms-flashcards__card',
} );
